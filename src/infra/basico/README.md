# Como executar um exemplo simples do Docker:
## Build
`$ docker build -t nome_container:tag_de_versao .`
## Run
`$ docker run -it nome_container:tag_de_versao`
O -it vai executar e lhe colocar num terminal interativo.